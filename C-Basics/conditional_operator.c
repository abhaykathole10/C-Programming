#include<stdio.h>
int main(){

    int i;
    printf("Enter i: ");
    scanf("%d",&i);

    //"?" and ":" are called the conditional operators
    //This is also a ternary function format

    (i==1 ? printf("Abhay"):printf("Emily"));

    return 0;
}
